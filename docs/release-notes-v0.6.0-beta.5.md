# RokidHub Codex v0.6.0-beta.5

This Windows Connector hotfix makes the one-time pairing code appear
immediately. The packaged child process now uses line-buffered UTF-8 output and
explicitly flushes the pairing messages while the session is still active.

The Android plugin and its existing `v0.6.0-beta.3` APK are unchanged.
